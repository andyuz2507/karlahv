/** @type {import('next').NextConfig} */
const nextConfig = {
  // Backend habilitado: API routes y admin panel
  // Para export estático, cambiar a output: 'export' y deshabilitar /admin
}

module.exports = nextConfig
